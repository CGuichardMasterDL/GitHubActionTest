name: CrudJS Lastest Build
on:
    push:
        branches:
            - master
jobs:
    build:
        name: Lastest Build
        runs-on: ubuntu-latest
        strategy:
          matrix:
            node-version: [12.x]
        steps:
            - uses: actions/checkout@v2
            - name: Use Node.js
              uses: actions/setup-node@v1
              with:
                node-version: ${{ matrix.node-version }}
            - run: npm install
            - run: npm run build:dist --if-present
            - name: Build to lastest-build branch
              uses: Embraser01/update-git-branch-action@v1.0.0
              with:
                branch: lastest-build
                githubToken: ${{ secrets.PAT_TOKEN }} # Github Token
            - run: npm run build:docs --if-present
            - name: Deploy to github pages
              uses: JamesIves/github-pages-deploy-action@releases/v3
              with:
                ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
                BRANCH: gh-pages
                FOLDER: docs
